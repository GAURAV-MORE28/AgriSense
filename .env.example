# Port Configuration
# Change these if you have port conflicts
FRONTEND_PORT=3000
BACKEND_PORT=4000
ML_PORT=5000
POSTGRES_PORT=5432
REDIS_PORT=6379

# React App Configuration (Must match external ports if running locally)
REACT_APP_API_URL=http://localhost:4000/api/v1
REACT_APP_ML_SERVICE_URL=http://localhost:5000

# Backend Secrets
JWT_SECRET=development-secret-key-change-in-prod
NODE_ENV=development
